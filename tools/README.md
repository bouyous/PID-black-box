# tools/

Placez ici `blackbox_decode.exe` (outil officiel Betaflight).

**Téléchargement :** https://github.com/betaflight/blackbox-tools/releases  
Choisissez la dernière release, prenez `blackbox_decode.exe` dans les assets Windows.

L'exécutable doit s'appeler exactement `blackbox_decode.exe`.

Ce fichier est exclu du git (voir `.gitignore`).
